/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['assets.aceternity.com', 'cdn.prod.website-files.com', 'fabrikbrands.com', 'inkbotdesign.com'],
    }
};

export default nextConfig;
